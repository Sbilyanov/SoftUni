using System;

namespace Test
{
    class Program
    {
        static void Main(string[] args)
        {
            string text = Console.ReadLine();
            PrintText(text);
        }

        private static void PrintText(string);
        {
            Console.WriteLine("I love" +text);
        }
    }
}
